.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/

# Run checkpoints are scratch, except the committed golden run that backs
# `make replay` as the offline demo fallback on a fresh clone.
runs/*
!runs/demo/
runs/demo/*.timing.json
