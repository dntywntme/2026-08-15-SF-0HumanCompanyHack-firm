# Tier 1 of the trust model: the Builder pushes branches but cannot merge.
# A Code Owner is the only identity that approves changes to main.
#
# Both maintainers are listed. GitHub ignores a CODEOWNERS entry for an account
# without access to this repository, so @qte77 needs at least read access here
# for that half of the rule to bind; until then @dntywntme carries it alone.
#
# This file is only load-bearing with branch protection enabled on main:
#   Settings -> Branches -> require a pull request, require review from
#   Code Owners, block force-push.
#
# See README.md ("Two actors") and docs/ARCHITECTURE.md#trust-model.

* @dntywntme @qte77
